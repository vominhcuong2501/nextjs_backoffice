export const PAGE_PATH = {
    DASHBOARD: '/dashboard'
}

export const PAGE_PATH_AUTH = {
    LOGIN: '/login'
}

export type PagePathKey = keyof typeof PAGE_PATH

export type PagePathAuthKey = keyof typeof PAGE_PATH_AUTH
